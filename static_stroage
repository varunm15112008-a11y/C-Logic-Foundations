#include <stdio.h>

void next(void);
static int counter = 7;

int main()
{
	while(counter<10)
	  {
	  	next();
	  	counter++;
	  }
	return 0;
}

void next()
 {
 	static  int iteration = 13;
 	iteration++;
 	printf("interation = %d and counter = %d\n",iteration,counter);
 	 	
 }
